// next-i18next.config.js
/** @type {import('next-i18next').UserConfig} */
module.exports = {
  i18n: {
    // Estos son los idiomas que soportarás
    locales: ['es', 'en'],
    // Este es el idioma por defecto
    defaultLocale: 'es',
  },
};